services:
  - type: web
    name: nasa-apod-explorer
    env: node
    rootDir: .
    buildCommand: |
      cd frontend && npm ci && npm run build
      cd ../backend && npm ci
    startCommand: node backend/server.js
    envVars:
      - key: NASA_API_KEY
        sync: false
