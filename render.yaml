services:
  - type: web
    name: nasa-apod-explorer
    env: node
    rootDir: .
    buildCommand: |
      cd frontend && npm install && npm run build
      cd .. && npm install
    startCommand: node backend/server.js
    envVars:
      - key: NASA_API_KEY
        sync: false
