services:
  - type: web
    name: apod-explorer
    env: node
    buildCommand: |
      cd frontend
      npm install
      npm run build
      cd ..
      cd backend
      npm install
    startCommand: node server.js
    rootDir: backend
    envVars:
      - key: PORT
        value: 5000
